apiVersion: batch/v1
kind: Job
metadata:
  name: echo_job
spec:
  template:
    metadata:
        name: echo_job
        labels: {
           app: controlm,
           controlm-order-id-internal-label : echo_job
        }     

    spec:
      containers:
      - name: almalinux8
        image: almalinux:8
        command: [ "echo", "{{job_yaml_file_params:masterid}}" ]
      restartPolicy: Never
      
